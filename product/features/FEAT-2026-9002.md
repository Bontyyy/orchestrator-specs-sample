# FEAT-2026-9002 — Widgets metadata endpoint (fixture for F2 backup walkthrough)

## Feature summary

Fixture feature seeded for the Phase 3 WU 3.6 F2 backup walkthrough (qa-curation stress). Defines a metadata endpoint whose `AC-2` has been intentionally removed from this file — the corresponding test in `/product/test-plans/FEAT-2026-9002.md` references `AC-2` and is therefore **an orphan candidate** for qa-curation's retirement path. An open `qa_regression_filed` event is also seeded on that test_id to exercise **open-regression protection** — qa-curation should REFUSE to retire the orphan. **Not a real product feature.**

## Scope

- Expose `GET /widgets/metadata` returning a widget-count summary.

## Out of scope

- Historical records.

## Acceptance criteria

- **AC-1** — Metadata endpoint returns the current widget count as a JSON object.

*Note: `AC-2` (previously covering `last_updated` timestamp) was removed; the existing test plan still references it, making that test an orphan.*

## Related specs

Walkthrough fixture only. See the F2 backup walkthrough log in the orchestrator repo.
