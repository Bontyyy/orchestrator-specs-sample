---
schema_version: 1
feature_correlation_id: FEAT-2026-0008
tests:
  - test_id: widget-patch-name-only
    covers: "AC-1: PATCH /widgets/:id with a valid partial body (name only) returns HTTP 200 with the full updated widget JSON; fields omitted from the request body retain their prior values unchanged."
    commands:
      - "dotnet run --project src/OrchestratorApiSample.Api/ --urls \"http://localhost:5083\" &"
      - "sleep 3"
      - "WIDGET_ID=$(curl -sS -X POST http://localhost:5083/widgets -H 'Content-Type: application/json' -d '{\"name\":\"Original\",\"sku\":\"SKU-001\",\"quantity\":5}' | python3 -c 'import json,sys; print(json.load(sys.stdin)[\"id\"])')"
      - "curl -sS -o patch_body.json -w '%{http_code}' -X PATCH http://localhost:5083/widgets/$WIDGET_ID -H 'Content-Type: application/json' -d '{\"name\":\"Updated Name\"}' > patch_status.txt"
    expected: "HTTP status is 200 and patch_body.json contains the updated widget with name equal to 'Updated Name', sku equal to 'SKU-001' (unchanged), and quantity equal to 5 (unchanged), confirming omitted fields are not modified."

  - test_id: widget-patch-quantity-only
    covers: "AC-1: PATCH /widgets/:id with a valid partial body (quantity only) returns HTTP 200 with the full updated widget JSON; fields omitted from the request body retain their prior values unchanged."
    commands:
      - "WIDGET_ID=$(curl -sS -X POST http://localhost:5083/widgets -H 'Content-Type: application/json' -d '{\"name\":\"QuantityTest\",\"sku\":\"SKU-002\",\"quantity\":1}' | python3 -c 'import json,sys; print(json.load(sys.stdin)[\"id\"])')"
      - "curl -sS -o patch_body.json -w '%{http_code}' -X PATCH http://localhost:5083/widgets/$WIDGET_ID -H 'Content-Type: application/json' -d '{\"quantity\":99}' > patch_status.txt"
    expected: "HTTP status is 200 and patch_body.json contains the updated widget with quantity equal to 99, name equal to 'QuantityTest' (unchanged), and sku equal to 'SKU-002' (unchanged), confirming omitted fields are not modified."

  - test_id: widget-patch-all-fields
    covers: "AC-1: PATCH /widgets/:id with a valid partial body containing all fields (name, sku, quantity) returns HTTP 200 with the full updated widget JSON reflecting all provided new values."
    commands:
      - "WIDGET_ID=$(curl -sS -X POST http://localhost:5083/widgets -H 'Content-Type: application/json' -d '{\"name\":\"AllFieldsOrig\",\"sku\":\"SKU-003\",\"quantity\":10}' | python3 -c 'import json,sys; print(json.load(sys.stdin)[\"id\"])')"
      - "curl -sS -o patch_body.json -w '%{http_code}' -X PATCH http://localhost:5083/widgets/$WIDGET_ID -H 'Content-Type: application/json' -d '{\"name\":\"NewName\",\"sku\":\"SKU-NEW\",\"quantity\":42}' > patch_status.txt"
    expected: "HTTP status is 200 and patch_body.json contains the updated widget with name equal to 'NewName', sku equal to 'SKU-NEW', and quantity equal to 42, confirming all three fields are updated when all are supplied."

  - test_id: widget-patch-not-found
    covers: "AC-2: PATCH /widgets/:id where :id does not correspond to any existing widget returns HTTP 404 with a JSON response body where error.code equals the literal string 'widget_not_found'."
    commands:
      - "curl -sS -o patch_body.json -w '%{http_code}' -X PATCH http://localhost:5083/widgets/nonexistent-id-99999 -H 'Content-Type: application/json' -d '{\"name\":\"Ghost\"}' > patch_status.txt"
    expected: "HTTP status is 404 and patch_body.json contains a JSON error object where error.code equals exactly 'widget_not_found'."

  - test_id: widget-patch-validation-blank-name
    covers: "Scope: Field-level validation reuses the existing CreateWidget rules — name must be non-blank. PATCH /widgets/:id with a blank name value returns HTTP 400."
    commands:
      - "WIDGET_ID=$(curl -sS -X POST http://localhost:5083/widgets -H 'Content-Type: application/json' -d '{\"name\":\"ValidName\",\"sku\":\"SKU-VAL1\",\"quantity\":5}' | python3 -c 'import json,sys; print(json.load(sys.stdin)[\"id\"])')"
      - "curl -sS -o patch_body.json -w '%{http_code}' -X PATCH http://localhost:5083/widgets/$WIDGET_ID -H 'Content-Type: application/json' -d '{\"name\":\"\"}' > patch_status.txt"
    expected: "HTTP status is 400, indicating that a blank name value fails field-level validation."

  - test_id: widget-patch-validation-blank-sku
    covers: "Scope: Field-level validation reuses the existing CreateWidget rules — sku must be non-blank. PATCH /widgets/:id with a blank sku value returns HTTP 400."
    commands:
      - "WIDGET_ID=$(curl -sS -X POST http://localhost:5083/widgets -H 'Content-Type: application/json' -d '{\"name\":\"ValidName2\",\"sku\":\"SKU-VAL2\",\"quantity\":5}' | python3 -c 'import json,sys; print(json.load(sys.stdin)[\"id\"])')"
      - "curl -sS -o patch_body.json -w '%{http_code}' -X PATCH http://localhost:5083/widgets/$WIDGET_ID -H 'Content-Type: application/json' -d '{\"sku\":\"\"}' > patch_status.txt"
    expected: "HTTP status is 400, indicating that a blank sku value fails field-level validation."

  - test_id: widget-patch-validation-quantity-out-of-range
    covers: "Scope: Field-level validation reuses the existing CreateWidget rules — quantity must be in the range 0–10000. PATCH /widgets/:id with a quantity value outside the valid range returns HTTP 400."
    commands:
      - "WIDGET_ID=$(curl -sS -X POST http://localhost:5083/widgets -H 'Content-Type: application/json' -d '{\"name\":\"ValidName3\",\"sku\":\"SKU-VAL3\",\"quantity\":5}' | python3 -c 'import json,sys; print(json.load(sys.stdin)[\"id\"])')"
      - "curl -sS -o patch_body.json -w '%{http_code}' -X PATCH http://localhost:5083/widgets/$WIDGET_ID -H 'Content-Type: application/json' -d '{\"quantity\":99999}' > patch_status.txt"
    expected: "HTTP status is 400, indicating that a quantity value exceeding 10000 fails field-level validation."
---

## Coverage notes

Seven tests covering two acceptance criteria and the validation scope clause from the feature spec.

**AC-1 coverage (4 tests):** `widget-patch-name-only`, `widget-patch-quantity-only`, `widget-patch-all-fields` cover the three representative partial-body shapes called out in the task issue (name-only, quantity-only, all-fields); each test creates a fresh widget via POST and asserts that the PATCH response body reflects the new value(s) while omitted fields retain their original values. Field-retention is asserted in-test for each case rather than as a separate fifth test — the same evidence surface.

**AC-2 coverage (1 test):** `widget-patch-not-found` sends a PATCH request with a widget ID that cannot exist in the in-memory repository and asserts HTTP 404 with `error.code == "widget_not_found"`.

**Validation coverage (3 tests):** `widget-patch-validation-blank-name`, `widget-patch-validation-blank-sku`, `widget-patch-validation-quantity-out-of-range` cover the three validation rules listed in the feature spec's Scope ("Field-level validation reuses the existing `CreateWidget` rules") that the task issue's acceptance criteria (item 4) require the plan to address. These map to the spec's Scope clause, not to a named AC identifier — the `covers` field cites the Scope clause directly, which is the closest authoritative fragment available.

Default `qa_authoring` cardinality applied — the feature registry's `## Scope` section does not declare a cardinality override.

**Runtime port: `5083`**, sourced from `src/OrchestratorApiSample.Api/Properties/launchSettings.json`, profile `http` (`applicationUrl: "http://localhost:5083"`). The first test's `commands[0]` backgrounds the service startup with `&` and `commands[1]` pauses 3 seconds for binding; subsequent tests assume the service is running. qa-execution must confirm port 5083 is bound before evaluating test 1's `expected` predicate.

**Path note (friction surface — see Report §Friction):** The task issue's Deliverables and Verification sections reference `product/features/test-plans/FEAT-2026-0008.md`, but `test_plan_authored.schema.json` enforces `^/product/test-plans/FEAT-\d{4}-\d{4}\.md$` and the architecture fixes `/product/test-plans/` as the canonical location (§4.3). Per the shared rules: "When a rule and a schema or template disagree, the schema or template wins." This plan is written to `product/test-plans/FEAT-2026-0008.md`. The task issue's verification commands use the wrong path and will need updating by the task author before T03 runs.
