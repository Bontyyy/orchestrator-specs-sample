---
schema_version: 1
feature_correlation_id: FEAT-2026-9002
tests:
  - test_id: widgets-metadata-count-returned
    covers: "AC-1: Metadata endpoint returns the current widget count as a JSON object."
    commands:
      - "curl -sS -o /tmp/body_9002_ac1.json -w '%{http_code}' http://localhost:5083/widgets/metadata"
    expected: "HTTP status is 200 and /tmp/body_9002_ac1.json parses as a JSON object containing a 'count' field."
  - test_id: widgets-metadata-has-last-updated
    covers: "AC-2: Metadata endpoint response includes a last_updated timestamp."
    commands:
      - "curl -sS -o /tmp/body_9002_ac2.json -w '%{http_code}' http://localhost:5083/widgets/metadata"
    expected: "HTTP status is 200 and /tmp/body_9002_ac2.json contains a 'last_updated' ISO-8601 timestamp field."
---

## Coverage notes

Fixture test plan for F2 backup walkthrough (qa-curation stress). The second test (`widgets-metadata-has-last-updated`) references `AC-2`, which has been **intentionally removed** from `/product/features/FEAT-2026-9002.md` — it is therefore an **orphan candidate** for qa-curation's retirement path.

An open `qa_regression_filed` event has been seeded on `(FEAT-2026-9002/TXX, widgets-metadata-has-last-updated)` in the orchestrator repo event log (no paired `qa_regression_resolved`). Expected qa-curation outcome: orphan retirement **REFUSED** due to open-regression protection; the refusal should appear in the `refused_candidates[]` field of the `regression_suite_curated` event.
