---
schema_version: 1
feature_correlation_id: FEAT-2026-0007
tests:
  - test_id: widgets-list-default-page-size-50
    covers: "AC-1: GET /widgets with no page_size query parameter returns HTTP 200 with a JSON array of up to 50 widget objects."
    commands:
      - "curl -sS -o /tmp/body_ac1.json -w '%{http_code}' http://localhost:5083/widgets"
    expected: "HTTP status is 200 and /tmp/body_ac1.json parses as a JSON array containing at most 50 widget objects."
  - test_id: widgets-list-explicit-page-size-honored
    covers: "AC-2: GET /widgets?page_size=N with 1 <= N <= 500 returns HTTP 200 with a JSON array of up to N widget objects."
    commands:
      - "curl -sS -o /tmp/body_ac2.json -w '%{http_code}' 'http://localhost:5083/widgets?page_size=10'"
    expected: "HTTP status is 200 and /tmp/body_ac2.json parses as a JSON array containing at most 10 widget objects."
  - test_id: widgets-list-page-size-over-limit-rejected
    covers: "AC-3: GET /widgets?page_size=N with N > 500 returns HTTP 400 with a JSON response body shaped as {\"error\": {\"code\": \"page_size_over_limit\", \"message\": \"...\"}}. The error.code field must equal the literal string page_size_over_limit."
    commands:
      - "curl -sS -o /tmp/body_ac3.json -w '%{http_code}' 'http://localhost:5083/widgets?page_size=501'"
    expected: "HTTP status is 400 and /tmp/body_ac3.json contains a JSON object with error.code equal to the literal string \"page_size_over_limit\"."
---

## Coverage notes

Three tests, one per acceptance criterion. Default `qa_authoring` cardinality applied — the feature's `## Scope` section states "three tests expected under the default cardinality convention", which is confirmatory of the default, not a collapse.

All `commands[]` target `http://localhost:5083/widgets`. Port 5083 is the http profile declared in `launchSettings.json` for the api-sample project; qa-execution should re-confirm the port is active before running the plan (F3.3 mitigation).

AC-2 uses `page_size=10` as a representative in-range value (well within 1–500). The test is parameterised by the value 10 rather than the boundary values 1 or 500 to keep the plan simple at v1; boundary-value expansion is deferred to Phase 4's structured predicate layer. qa-execution should accept any `page_size` value satisfying `1 <= N <= 500` as equivalent for this test.

AC-3 uses `page_size=501` (one above the limit) as the canonical over-limit probe. The `error.code` literal `page_size_over_limit` is the load-bearing field the plan keys on; the `message` field is not asserted.
