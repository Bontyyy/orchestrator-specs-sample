---
schema_version: 1
feature_correlation_id: FEAT-2026-9001
tests:
  - test_id: widgets-listing-default-slice-50-count
    covers: "AC-1: Listing returns up to 50 widget objects by default."
    commands:
      - "curl -sS -o /tmp/body_9001.json -w '%{http_code}' http://localhost:5083/widgets"
    expected: "HTTP status is 200 and /tmp/body_9001.json parses as a JSON array containing at most 50 widget objects."
---

## Coverage notes

Fixture test plan for F2 backup walkthrough (qa-curation stress). This test's `covers` field intentionally overlaps semantically with `FEAT-2026-0007`'s `widgets-list-default-page-size-50` test (both cover "up to 50 widgets by default" behavior on `GET /widgets`). Expected qa-curation outcome: **dedup candidate** — the two tests should merge or one be retired in favor of the other.
