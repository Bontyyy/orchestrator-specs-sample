---
schema_version: 1
feature_correlation_id: FEAT-2026-0006
tests:
  - test_id: widgets-count-returns-total
    covers: "AC-1: GET /widgets/count returns HTTP 200 with a JSON response body {\"count\": N} where N is a non-negative integer equal to the total number of widgets currently persisted."
    commands:
      - "curl -sS -o body.json -w '%{http_code}' http://localhost:5000/widgets/count"
    expected: "HTTP status is 200 and body.json parses as a JSON object with a single field 'count' whose value is a non-negative integer equal to the number of widgets in the persisted store."
---

## Coverage notes

One test, one acceptance criterion. Default `qa_authoring` cardinality applied — the feature's `## Scope` section is silent on cardinality override, so the default (one test per behavior) applies. FEAT-2026-0006 has a single AC (AC-1) and a single behavior, producing a plan of exactly one test entry.

The `commands` field assumes the API runs locally on port 5000, consistent with the .NET development server default in `Bontyyy/orchestrator-api-sample`. The `qa-execution` skill (T03) is responsible for confirming the actual port at execution time; if the endpoint is unreachable at this address, T03 should adjust the command to match the actual host/port before running.
