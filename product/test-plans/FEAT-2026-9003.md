---
schema_version: 1
feature_correlation_id: FEAT-2026-9003
tests:
  - test_id: widgets-archive-id-soft-deleted
    covers: "AC-1: DELETE /widgets/archive/{id} returns HTTP 204 when the archive succeeds."
    commands:
      - "curl -sS -o /dev/null -w '%{http_code}' -X DELETE http://localhost:5083/widgets/archive/test-id-1"
    expected: "HTTP status is 204 (No Content)."
---

## Coverage notes

Fixture test plan for F2 backup walkthrough (qa-curation stress).

rename-request: widgets-archive-old-variant → widgets-archive-id-soft-deleted

The current `test_id` (`widgets-archive-old-variant`) uses an ad-hoc name. The `rename-request:` line above exercises qa-curation's rename detection path — the skill should pick up the request and include it in its PR as a rename candidate (retire-old + add-new pattern per qa-curation SKILL.md). Expected qa-curation outcome: **rename candidate** accepted (no open regression on this `test_id` blocks it).
